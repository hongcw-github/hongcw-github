# 보안 정책 및 사고 대응 계획 (Security Policy & Incident Response Plan)

본 문서는 Amazon SP-API(Selling Partner API)를 통해 취득한 데이터("Amazon Information")를
안전하게 다루기 위한 보안 통제와 사고 대응 절차를 정의한다. Amazon Data Protection Policy
및 Acceptable Use Policy 준수를 목적으로 한다.

## 1. 데이터 범위

- 본 애플리케이션은 **본인 셀러 계정(RLSW)의 데이터만** 읽기 전용으로 조회한다.
- 취급 데이터: 주문, FBA 재고, 정산/재무 이벤트, 판매 지표.
- **개인식별정보(PII, Restricted roles)는 요청하지도 취급하지도 않는다.**
- 데이터는 **내부 비즈니스 분석 용도로만** 사용하며, 외부에 공유·판매·이전하지 않는다.

## 2. 자격증명 관리 (Credential Handling)

- 모든 자격증명(LWA Client ID/Secret, Refresh Token)은 **환경변수(`.env`)** 로만 보관한다.
- `.env` 는 `.gitignore` 에 등록되어 **버전 관리·공개 저장소에 절대 커밋되지 않는다.**
- 코드에 자격증명을 **하드코딩하지 않는다.**
- 자격증명을 타인과 **공유하지 않는다.**
- 의심스러운 노출 발생 시 Seller Central 에서 **즉시 토큰을 폐기/재발급** 한다.

## 3. 접근 통제 (Access Control)

- Amazon Information 에 대한 접근은 **직무상 필요한 사용자로 한정**한다 (현재: 운영자 본인).
- 모든 관련 계정(Seller Central, 이메일, 호스팅)에 **다단계 인증(MFA)** 을 적용한다.
- 비밀번호는 **12자 이상 + 특수문자** 를 사용하고 **연 1회 이상 교체** 한다.

## 4. 암호화 (Encryption)

- SP-API 통신은 **TLS/HTTPS** 로만 이루어진다 (전송 중 암호화).
- 로컬에 캐시·저장하는 데이터가 있을 경우, 저장 매체는 **디스크 암호화**를 적용한다.

## 5. 네트워크 보안

- 운영 환경에 **방화벽 및 안티바이러스/안티멀웨어**를 활성화한다.
- 불필요한 외부 노출 포트를 차단한다.

## 6. 사고 대응 계획 (Incident Response Plan)

### 역할
- **사고 책임자(Incident Owner)**: 운영자 본인 — 탐지·대응·통지 총괄.

### 절차
1. **탐지**: 비정상 접근, 자격증명 노출, 데이터 유출 의심 징후 모니터링.
2. **차단**: 즉시 영향받은 자격증명 폐기/재발급, 접근 차단.
3. **조사**: 영향 범위 및 원인 파악.
4. **통지**: 보안 사고 발생을 인지 후 **24시간 이내** 에
   **security@amazon.com** 으로 신고한다.
5. **복구 및 재발 방지**: 통제 보완 후 정상화.

### 검토 주기
- 본 계획은 **최소 6개월마다 1회** 검토·갱신한다.
- 최근 검토일: 2026-06-23

## 7. 데이터 공유 / 외부 출처

- **외부 공유 대상: 없음.** Amazon Information 을 어떤 외부 당사자와도 공유하지 않는다.
- **외부 취득 출처: 없음.** Amazon Information 은 오직 Amazon SP-API 에서 직접 취득한다.
